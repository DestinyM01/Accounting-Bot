# Apply once to the cluster:
#   kubectl apply -f web/k8s/argocd-app.yaml -n argocd
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: accounting-web
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/DestinyM01/Accounting-Bot
    targetRevision: main
    path: web/k8s/
    directory:
      exclude: "argocd-app.yaml"

  destination:
    server: https://kubernetes.default.svc
    namespace: accounting-bot

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 10s
        maxDuration: 3m
        factor: 2
